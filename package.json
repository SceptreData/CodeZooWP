{
  "name": "codezoo",
  "version": "1.0.0",
  "description": "Wordpress Theme for DMIT2032 - Content Management Systems",
  "main": "index.php",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run build && npx node-sass -w sass/ -o ./",
    "build": "npx node-sass sass/style.scss -o ./"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "gutenberg-web-type": "^1.2.3",
    "node-sass": "^4.13.0"
  },
  "devDependencies": {
    "grunt": "^1.5.3"
  }
}
